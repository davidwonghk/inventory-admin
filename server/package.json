{
  "name": "inventory-admin-backend",
  "version": "0.0.0",
  "private": true,

  "dependencies": {
    "async": "^2.6.0",
    "dotenv": "^5.0.1",
    "express": "^4.16.3",
    "moment": "^2.22.1",
    "mongodb": "^3.0.7"
  },
  "scripts": {
	"client": "cd ../client && yarn start",
	"build": "cd ../client && yarn install && yarn run build && cd ../server",
	"server": "nodemon ./bin/www",
	"start": "nodemon ./bin/www",
  },
  "devDependencies": {
    "nodemon": "^1.17.4"
  }
}
